{
  "name": "renao-kargo-takip-api",
  "version": "1.0.0",
  "private": true,
  "description": "Verify Shopify order number and email, then return fulfillment tracking details.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "check": "node --check index.js && node --check spotify.js"
  },
  "dependencies": {
    "node-fetch": "^2.7.0",
    "pngjs": "^7.0.0",
    "qrcode": "^1.5.4"
  },
  "engines": {
    "node": ">=18"
  }
}
